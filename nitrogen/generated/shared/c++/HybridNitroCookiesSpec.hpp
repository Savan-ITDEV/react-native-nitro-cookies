///
/// HybridNitroCookiesSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Cookie` to properly resolve imports.
namespace margelo::nitro::nitrocookies { struct Cookie; }

#include "Cookie.hpp"
#include <vector>
#include <string>
#include <NitroModules/Promise.hpp>
#include <optional>

namespace margelo::nitro::nitrocookies {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroCookies`
   * Inherit this class to create instances of `HybridNitroCookiesSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroCookies: public HybridNitroCookiesSpec {
   * public:
   *   HybridNitroCookies(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroCookiesSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroCookiesSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroCookiesSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::vector<Cookie> getSync(const std::string& url) = 0;
      virtual bool setSync(const std::string& url, const Cookie& cookie) = 0;
      virtual bool setFromResponseSync(const std::string& url, const std::string& value) = 0;
      virtual bool clearByNameSync(const std::string& url, const std::string& name) = 0;
      virtual std::shared_ptr<Promise<bool>> set(const std::string& url, const Cookie& cookie, std::optional<bool> useWebKit) = 0;
      virtual std::shared_ptr<Promise<std::vector<Cookie>>> get(const std::string& url, std::optional<bool> useWebKit) = 0;
      virtual std::shared_ptr<Promise<bool>> clearAll(std::optional<bool> useWebKit) = 0;
      virtual std::shared_ptr<Promise<bool>> setFromResponse(const std::string& url, const std::string& value) = 0;
      virtual std::shared_ptr<Promise<std::vector<Cookie>>> getFromResponse(const std::string& url) = 0;
      virtual std::shared_ptr<Promise<std::vector<Cookie>>> getAll(std::optional<bool> useWebKit) = 0;
      virtual std::shared_ptr<Promise<bool>> clearByName(const std::string& url, const std::string& name, std::optional<bool> useWebKit) = 0;
      virtual std::shared_ptr<Promise<void>> flush() = 0;
      virtual std::shared_ptr<Promise<bool>> removeSessionCookies() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroCookies";
  };

} // namespace margelo::nitro::nitrocookies
